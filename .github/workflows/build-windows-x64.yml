name: Build Windows x64

on:
  workflow_call:

jobs:
  build:
    name: Build (Windows / x64)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Setup sccache
        uses: mozilla/sccache-action@v0.0.8

      - name: Configure Compiler Caches
        shell: bash
        run: |
          echo "PYTHONUNBUFFERED=1" >> $GITHUB_ENV
          echo "CC=sccache cl" >> $GITHUB_ENV
          echo "CXX=sccache cl" >> $GITHUB_ENV

      - name: Cache Nuitka
        uses: actions/cache@v4
        with:
          path: |
            ~/AppData/Local/Nuitka/Nuitka/Cache
            .nuitka
          key: nuitka-windows-x64-${{ hashFiles('**/*.py', 'pyproject.toml') }}
          restore-keys: |
            nuitka-windows-x64-

      - name: Install dependencies
        run: pip install nuitka ordered-set zstandard -e .

      - name: Build binary
        shell: pwsh
        run: |
          python -m nuitka `
            --onefile `
            --output-filename=tmd-windows-x64.exe `
            --include-package=src `
            --include-package=backend `
            --follow-imports `
            --assume-yes-for-downloads `
            --jobs=4 `
            --windows-icon-from-ico=assets/tmd.ico `
            tmd_runner.py

      - name: Smoke test
        shell: bash
        run: |
          ./tmd-windows-x64.exe --version
          ./tmd-windows-x64.exe --help

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tmd-windows-x64.exe
          path: tmd-windows-x64.exe
